/* eslint-disable */
/* Import actions to dispatch */

export function myThunk(myThunkParams) {
  return async (dispatch) => {
    // Write your asynchronous code here

    // Dispatch your action when done
    // dispatch(myActionToDispatch());
  };
}

export default fetchFormDefinition;
